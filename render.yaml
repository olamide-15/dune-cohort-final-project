services:
  - type: web
    name: smartstudy
    runtime: python
    buildCommand: pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn smartstudy.wsgi
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
